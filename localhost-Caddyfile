{$DOMAIN:localhost} {
	# Proxy API requests to Laravel backend
	handle /api/* {
		reverse_proxy backend:8000
	}

	# Proxy everything else to React frontend (Caddy)
	handle {
		reverse_proxy frontend:3000
	}
}
